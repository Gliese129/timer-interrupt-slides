# Timer Interrupt Slides

Static GitHub Pages build for the ESP32 timer interrupt education slides.

## View

Open `index.html`, or publish this repository with GitHub Pages from the `main` branch root.

Expected GitHub Pages URL after publishing:

`https://gliese129.github.io/timer-interrupt-slides/`
